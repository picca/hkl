include_directories(${HKL_SOURCE_DIR}/include)
link_directories (${HKL_BINARY_DIR}/src) 
file(GLOB sources *.c)
add_executable(alltest ${sources})
target_link_libraries (alltest hkl gsl gslcblas) 
add_test(test alltest)
